spring:
  application:
    name: Platzi pizzeria jpa
  datasource:
    url: jdbc:mysql://localhost:3306/platzi-pizzeria-sc?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org.springframework.security.web.*: debug # Show logs from spring security